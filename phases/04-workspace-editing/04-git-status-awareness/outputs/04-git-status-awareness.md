# Git guard

**Phase:** Workspace Editing
**Lesson:** Git Status Awareness

## Purpose

This artifact captures the output expected from the lesson demo.

## Evidence Contract

- The demo must run with `pnpm exec tsx phases/04-workspace-editing/04-git-status-awareness/code/demo.ts`.
- The demo must print `04-git-status-awareness: Git guard`.
- The demo must end with `status: ready`.

