# Refresh trace

**Phase:** MCP 与插件
**Lesson:** 动态 Tool 刷新

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/08-mcp-and-plugins/05-dynamic-tool-refresh/code/demo.ts` 运行。
- demo 必须输出 `05-dynamic-tool-refresh: Refresh trace`。
- demo 必须以 `status: ready` 收尾。

