# Local server

**Phase:** MCP 与插件
**Lesson:** 本地 MCP Server 进程

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/08-mcp-and-plugins/02-local-mcp-server-process/code/demo.ts` 运行。
- demo 必须输出 `02-local-mcp-server-process: Local server`。
- demo 必须以 `status: ready` 收尾。

