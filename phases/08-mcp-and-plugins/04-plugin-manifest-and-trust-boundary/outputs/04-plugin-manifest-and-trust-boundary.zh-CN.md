# Plugin manifest

**Phase:** MCP 与插件
**Lesson:** Plugin Manifest 与信任边界

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/08-mcp-and-plugins/04-plugin-manifest-and-trust-boundary/code/demo.ts` 运行。
- demo 必须输出 `04-plugin-manifest-and-trust-boundary: Plugin manifest`。
- demo 必须以 `status: ready` 收尾。

