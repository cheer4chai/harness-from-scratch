# Tool interface

**Phase:** 工具运行时
**Lesson:** Tool 接口与 JSON Schema 输入

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/03-tool-runtime/01-tool-interface-and-json-schema-inputs/code/demo.ts` 运行。
- demo 必须输出 `01-tool-interface-and-json-schema-inputs: Tool interface`。
- demo 必须以 `status: ready` 收尾。

