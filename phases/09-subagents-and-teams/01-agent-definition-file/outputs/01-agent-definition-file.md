# Agent manifest

**Phase:** Subagents & Teams
**Lesson:** Agent Definition File

## Purpose

This artifact captures the output expected from the lesson demo.

## Evidence Contract

- The demo must run with `pnpm exec tsx phases/09-subagents-and-teams/01-agent-definition-file/code/demo.ts`.
- The demo must print `01-agent-definition-file: Agent manifest`.
- The demo must end with `status: ready`.

