# Agent manifest

**Phase:** 子 Agent 与团队
**Lesson:** Agent Definition 文件

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/09-subagents-and-teams/01-agent-definition-file/code/demo.ts` 运行。
- demo 必须输出 `01-agent-definition-file: Agent manifest`。
- demo 必须以 `status: ready` 收尾。

