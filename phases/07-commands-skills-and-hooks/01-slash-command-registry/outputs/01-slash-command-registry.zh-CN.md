# Command registry

**Phase:** 命令、Skills 与 Hooks
**Lesson:** Slash Command Registry

## Purpose

这个 artifact 记录本章 demo 应该产出的结果。

## Evidence Contract

- demo 必须能通过 `pnpm exec tsx phases/07-commands-skills-and-hooks/01-slash-command-registry/code/demo.ts` 运行。
- demo 必须输出 `01-slash-command-registry: Command registry`。
- demo 必须以 `status: ready` 收尾。

